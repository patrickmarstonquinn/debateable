<div class="page-header">
  <h3>

    <%= @user.name %>



  </h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>Contact Email</dt>
      <dd><%= @user.email %></dd>

      <!-- <dt>Picture</dt>
      <dd><img src="<%= @user.picture %>" class="img-responsive"></dd> -->

      <!-- <dt>Name</dt>
      <dd><%= @user.name %></dd> -->

      <dt>Bio</dt>
      <dd><%= @user.bio %></dd>

    </dl>


  </div>
</div>

<!-- A User has many debates -->

<div class="row">
  <div class="col-md-12">
    <ul class="list-group">
      <li class="list-group-item list-group-item-info">
      <%= @user.name %>'s  Debates
      </li>

      <% @user.debates.each do |debate| %>
      <li class="list-group-item">
        <a href="/debates/<%= debate.id %>">
          <%= debate.topic %>
        </a>
        <div class="btn-group btn-group-xs pull-right">
          <a href="/debates/<%= debate.id %>" class="btn btn-primary">
            <i class="fa fa-search-plus"></i>
          </a>
          <%if current_user == debate.user %>
          <a href="/debates/<%= debate.id %>/edit" class="btn btn-warning">
            <i class="fa fa-edit"></i>
          </a>
          <a href="/delete_debate/<%= debate.id %>" class="btn btn-danger" rel="nofollow">
            <i class="fa fa-trash-o"></i>
          </a>
          <% end %>
        </div>
      </li>
      <% end %>
    </ul>
  </div>
</div>

<!-- A User has many comments -->

<div class="row">
  <div class="col-md-12">
    <ul class="list-group">
      <li class="list-group-item list-group-item-info">
        <%= @user.name %>'s Comments
      </li>

      <% @user.comments.each do |comment| %>
      <li class="list-group-item">
        On <a href="/comments/<%= comment.id %>">
          <%= Debate.find(comment).topic %>:
        </a>
          <%= comment.body %>

        <div class="btn-group btn-group-xs pull-right">
          <a href="/comments/<%= comment.id %>" class="btn btn-primary">
            <i class="fa fa-search-plus"></i>
          </a>
          <%if current_user == comment.user %>
          <a href="/comments/<%= comment.id %>/edit" class="btn btn-warning">
            <i class="fa fa-edit"></i>
          </a>
          <a href="/delete_comment/<%= comment.id %>" class="btn btn-danger" rel="nofollow">
            <i class="fa fa-trash-o"></i>
          </a>
          <% end %>
        </div>
      </li>
      <% end %>
    </ul>
  </div>
</div>

<!-- A User has many favorites -->

<div class="row">
  <div class="col-md-12">
    <ul class="list-group">
      <li class="list-group-item list-group-item-info">
        <%= @user.name %>'s Favorites
      </li>

      <% @user.favorites.each do |favorite| %>
      <li class="list-group-item">
        <a href="/favorites/<%= favorite.id %>">
          <%= Debate.find(favorite.debate_id).topic %>
        </a>
        <div class="btn-group btn-group-xs pull-right">
          <a href="/favorites/<%= favorite.id %>" class="btn btn-primary">
            <i class="fa fa-search-plus"></i>
          </a>
          <%if current_user == favorite.user %>
          <a href="/favorites/<%= favorite.id %>/edit" class="btn btn-warning">
            <i class="fa fa-edit"></i>
          </a>
          <a href="/delete_favorite/<%= favorite.id %>" class="btn btn-danger" rel="nofollow">
            <i class="fa fa-trash-o"></i>
          </a>
          <% end %>
        </div>
      </li>
      <% end %>
    </ul>

    <div class="btn-group btn-group-justified">
      <a href="/users" class="btn btn-primary">
        Back
      </a>
  </div>
</div>
